{
  "name": "when2riot-dev",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && firebase deploy"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.4.1"
  }
}